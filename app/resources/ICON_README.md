# App Icon (optional)

Drop a Windows `.ico` file here named `app_icon.ico` (256x256 multi-resolution
recommended) and `build.py` will automatically use it when building the .exe.
Without one, PyInstaller just uses its default icon — the app builds and
runs fine either way.
